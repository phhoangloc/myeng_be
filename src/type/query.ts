export type QueryType = {
    id?: number,
    username?: string,
    email?: string,
}
export type QueryPicType = {
    genre?: string,
    id?: number,
    name?: string,
    slug?: string,
    skip?: number,
    limit?: number
}